{
  "name": "my-site",
  "type": "module",
  "private": true,
  "scripts": {
    "build": "nuxt build",
    "dev": "nuxt dev",
    "generate": "nuxt generate && (robocopy ./output/public ./docs /E /MOVE /R:0 /W:0 || exit 0) && rd /s /q \".\\output\"",
    "preview": "nuxt preview",
    "postinstall": "nuxt prepare"
  },
  "dependencies": {
    "nuxt": "^4.3.1",
    "pilotui": "^1.28.1",
    "vue": "^3.5.28",
    "vue-router": "^4.6.4"
  },
  "devDependencies": {
    "shx": "^0.4.0"
  }
}
